{{ '<%= name %>.css' | asset_url | stylesheet_tag }}

<div class="<%= className %>"
     data-template="<%= name %>"
>
  <% if (sectionName) { %>{% section '<%= sectionName %>' %}<% } %>
</div>

{{ '<%= name %>.js' | asset_url | script_tag }}
