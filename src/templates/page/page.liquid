{{ 'page.js' | asset_url | script_tag }}

<style>
  {% render 'page.css' %}
</style>

<div class="page"
     data-template="page"
>
  <h1>{{ page.title }}</h1>

  <div class="rte">
    {{ page.content }}
  </div>
</div>
